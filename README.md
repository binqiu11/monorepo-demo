# A1 Project

## 📋 项目概述

这是一个基于 Pnpm 的前端 monorepo 项目，包含三个主要部分：

packages/a1 - a1 主应用（JavaScript + Vue 3）

packages/rmax - Reward 子应用/组件库（TypeScript + Vue 3）

packages/shared - 共享代码库（工具函数、公共组件、类型定义）

## 🏗️ Why Monorepo？

- shared 包提供可复用的组件、工具函数和类型定义

- 确保 UI 和功能的一致性

- packages/rmax 类型安全

- 统一的开发、构建、测试流程

- 相关功能可以同时开发、测试、发布

## 依赖关系图

```text
    ┌─────────────────┐
    │    Project A    │  (主应用)
    │  (JS + Vue 3)   │
    └────────┬────────┘
             │
    ┌────────▼────────┐    ┌─────────────────┐
    │    Project B    │    │     Shared      │
    │  (TS + Vue 3)   │◄───┤ (工具/组件/类型) │
    └────────┬────────┘    └─────────────────┘
             │                     │
             └─────────────────────┘
```

## 流程

```
根目录安装依赖 -> 启动 a1 -> 打包并监听 rmax/shared -> 修改 rmax/shared -> 刷新 a1
```

## 项目目录

```text
monorepo/
├── 📁 packages/                   # 所有子包目录
│   ├── 📁 a1/                     # 主应用 (JavaScript)
│   │   ├── 📁 src/                # 源码目录
│   │   │   ├── 📁 ...             # a1 开发资源
│   │   │   ├── App.vue            # 根组件
│   │   │   └── main.js            # 应用入口
│   │   ├── index.html             # HTML 模板
│   │   ├── vite.config.js         # Vite 配置
│   │   ├── 。。。
│   │   └── package.json           # 依赖配置
│   │
│   ├── 📁 rmax/                   # rmax 子应用/组件库 (TypeScript)
│   │   ├── 📁 src/                # 源码目录
│   │   │   ├── 📁 ...             # rmax 开发资源
│   │   │   └── index.ts           # rmax 库入口文件
│   │   ├── vite.config.ts         # Vite 配置（TypeScript）支持按需加载和 tree-shaking
│   │   ├── package.json           # 依赖配置
│   │   ├── ...
│   │   └── tsconfig.json          # TypeScript 配置
│   │
│   └── 📁 shared/                 # 共享代码库
│       ├── 📁 components/         # 公共 UI 组件
|       |   └── index.js           # 导出入口
│       ├── ...                    # 其他
│       └── package.json           # 依赖配置（peerDependencies），无需单独构建
│
├── package.json                 # 根目录配置（开发工具）
├── pnpm-workspace.yaml          # pnpm workspace 配置
├── nodemon.json                 # 监听 rmax、shared 更改并重新打包刷新
└── ...                          # 其他
```

## 🚀 快速开始

```shell
# 1. 安装根目录的开发工具依赖
# 2. 递归安装所有 workspace 包的依赖
# 3. 建立包之间的符号链接
pnpm init

# 1. 构建 rmax
# 2. 启动 rmax 的文件监听
# 3. 启动 a1 开发服务
pnpm dev

# 1. 构建 rmax
# 2. 构建 a1
pnpm build

# 添加依赖
# 在根目录添加开发工具
pnpm add -D <package-name>

# 在 a1 中添加依赖
cd packages/a1
pnpm add <package-name>

# 在 rmax 中添加依赖
cd packages/rmax
pnpm add <package-name>

# 在 shared 中添加 peer 依赖
cd packages/shared
pnpm add --save-peer <package-name>
```
